# build:
#   box: mikewhy/selenium-cucumber
#   steps:
#     - bundle-install
#     - rails-database-yml

#     - script:
#         name: setup database
#         code: bundle exec rake db:create db:migrate RAILS_ENV=test

#     - script:
#         name: rspec
#         code: bundle exec rspec

# deploy:
#   steps:
#     - heroku-deploy:
#         key-name: DEPLOYMENT_KEY
#         install-toolbelt: true
#         retry: true
#         run: rake db:migrate

# #         - npm-install
# #         - npm-test

box: busybox
build:
  box: donniewest/ubuntu12.04-rails-ruby-firefox-cucumber:0.0.4
  steps:
    - bundle-install
    - script:
        name: test version
        code: bundle exec ruby -v

deploy:
  # override busybox
  box: nodesource/node:trusty
  steps:
    - script:
        name: testing deploy
        code: node --version