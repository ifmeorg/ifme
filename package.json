{
  "name": "ifme",
  "private": true,
  "dependencies": {},
  "scripts": {
    "postinstall": "cd client && yarn install",
    "start": "foreman start -f Procfile.dev",
    "test": "cd client && yarn && cd .. && rspec"
  },
  "engines": {
    "node": "8.11.1"
  },
  "cacheDirectories": [
    "node_modules",
    "client/node_modules"
  ]
}
