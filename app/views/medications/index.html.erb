<div class="subtitle">
  <%= t('.subtitle') %>
</div>

<div class="spacer"></div>

<% if @medications.length > 0 %>
  <table>
    <thead>
      <tr>
        <th><%= t('.name') %></th>
        <th><%= t('.strength') %></th>
        <th><%= t('.quantity') %></th>
        <th><%= t('.dosage') %></th>
        <th><%= t('.refill') %></th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <% @medications.each do |medication| %>
        <tr>
          <td><%= link_to medication.name, medication %></td>
          <td><%= medication.strength.to_s + " " + medication.strength_unit %></td>
          <td><%= medication.total.to_s + " " + medication.total_unit %></td>
          <td><%= medication.dosage.to_s + " " + medication.dosage_unit %></td>
          <td><%= medication.refill %></td>
          <td>
            <i class="fa fa-pencil action"></i><%= link_to t('.edit'), edit_medication_path(medication) %>
            <i class="fa fa-trash-o action small_margin_left"></i><%= link_to t('.delete'), medication, method: :delete, data: { confirm: t('.confirm') } %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
You haven't saved any medications yet. Click <i class="fa fa-plus-circle"></i> to add some! 
<br>
<br>Here's an example of a saved medication! You can also write about your experience.

<div class="spacer"></div>

<table>
    <thead>
      <tr>
        <th><%= t('.name') %></th>
        <th><%= t('.strength') %></th>
        <th><%= t('.quantity') %></th>
        <th><%= t('.dosage') %></th>
        <th><%= t('.refill') %></th>
      </tr>
    </thead>

    <tbody>
      <tr>
        <td>Prozac</td>
        <td>10 mg</td>
        <td>2 tablets</td>
        <td>40 tablets</td>
        <td>05/31/2016</td>
      </tr>
    </tbody>
  </table>
<% end %>