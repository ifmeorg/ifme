<% if user_signed_in? && current_user.password_needs_update? %>
  <%= react_component('Modal', props: {
    title: t('pages.home.update_pwd_modal.title'),
    body: t('pages.home.update_pwd_modal.body', update_password_link: link_to(t('pages.home.update_pwd_modal.update_link'), edit_user_registration_path)),
    open: true
  }) %>
<% end %>
