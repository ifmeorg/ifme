
<h4>You are reporting this user/comment !</h4>
<%= form_for(:search, :url => {:action => "create", :ally_id => params[:ally_id], :comment_id => params[:comment_id], :commentable_type => params[:commentable_type]}, :method => "post") do |f| %>
   Why do you want to report?
    <%= text_field(:report, :comments) %>
     <%= f.submit 'Report', class: 'no_margin_right' %>
<% end %>