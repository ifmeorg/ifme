<div class="contentNavProfile">
  <div class="contentNavProfilePicture">
    <%= ProfilePicture.fetch(current_user.avatar.url, className: 'content_nav_profile_picture') %>
  </div>
  <div class="contentNavProfileInfo">
    <%= current_user.name %>
    <div class"contentNavProfileInfoLinks">
      <%= link_to(t('profile.index.title'), profile_index_path(uid: current_user.uid)) %>
      <%= link_to(t('account.singular'), edit_user_registration_path) %>
      <%= link_to(t('notifications.plural'), '', class: 'notifications_button') %>
    </div>
  </div>
</div>

<div class="contentNavLinks">
  <%= content_nav_link_to(t('moments.plural'), moments_path) %>
  <%= content_nav_link_to(t('categories.plural'), categories_path) %>
  <%= content_nav_link_to(t('moods.plural'), moods_path) %>
  <%= content_nav_link_to(t('strategies.plural'), strategies_path) %>
  <%= content_nav_link_to(t('medications.index.title'), medications_path) %>
  <%= content_nav_link_to(t('groups.plural'), groups_path) %>
  <%= content_nav_link_to(t('allies.index.title'), allies_path) %>
</div>

<div class="contentNavActions">
  <%= link_to(t('shared.header.signout'), destroy_user_session_path, method: :delete) %>
</div>